# # .github/workflows/ping.yml
# name: Ping Render Server

# on:
#   schedule:
#     - cron: '*/12 * * * *'  # every 12 minutes
#   workflow_dispatch:

# jobs:
#   ping:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Curl the server
#         run: curl -m 10 https://globalacademy.onrender.com || echo "Server didn't respond"
